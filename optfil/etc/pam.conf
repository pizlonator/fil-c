# /opt/fil/etc/pam.conf
#
# PAM configuration file (legacy format)
# Modern PAM uses /etc/pam.d/* instead - see pam.d/ directory
#
# This file is provided for compatibility but is typically not used.
